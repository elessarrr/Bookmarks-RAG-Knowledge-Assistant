fastapi
uvicorn
python-multipart
duckdb
ollama
beautifulsoup4
requests
pytest
httpx
pytest-asyncio
pytest-cov

# Local-embedding stack. Pinned as a matched set: leaving these unpinned lets a
# clean install resolve to transformers 5.x, whose tensor_parallel module
# references `torch` before importing it (NameError at import time), which breaks
# `import sentence_transformers` and every test that touches the embedder. This
# transformers 4.x / torch 2.2 combo is a well-tested line for all-MiniLM-L6-v2.
# numpy is held <2 because torch 2.2 requires it (and langchain constrains it too).
sentence-transformers==3.0.1
transformers==4.44.2
torch==2.2.2
numpy==1.26.4

# Ingestion pipeline dependencies (imported directly by app/ingestion/*,
# app/config.py, run.py -- previously missing from this file, which meant
# a genuinely clean `pip install -r requirements.txt` failed before the
# app or its tests could even import).
nltk
readability-lxml==0.8.1
lxml_html_clean
PyYAML
python-dotenv

# Eval harness (evals/) -- RAGAS + langchain-community for faithfulness /
# answer-relevance scoring. Pinned as a matched set: recent langchain-core
# (>=1.x) and recent langchain-community (0.4.x) are mutually incompatible
# with ragas 0.1.9's eager top-level import of ChatVertexAI, which recent
# langchain-community no longer bundles.
ragas==0.1.9
langchain==0.1.16
langchain-core==0.1.53
langchain-community==0.0.38
langchain-openai==0.1.6
openai==1.30.5
